(ns xesenrich.sim
  (:require [xesenrich.cpn :as cpn])
    (:import [xesenrich.cpn CPN]
           [xesenrich.cpn Place]
           [xesenrich.cpn Transition]
           [xesenrich.cpn Arc]))

; CPN sim packet
(defn init-sim
  [cpn token-map transfunc]
  )


(defn start-sim [cpn start function]
  )
